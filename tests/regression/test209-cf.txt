main
// cf
int x, y, z;
bool a, b;
{
    y = 2;
    x = y * 2;
    x = 2 * y;
    x = x + 2 * y + x;
    x = y * 1;
    x = y * z; // z uninitialized
    x = y * 0;
    x = y ^ 0;
    x = 0 / y;
    x = y - y;
    x = y / y;
    x = y + 0;
    y += 1;
    z = 2*(x + y) - y;

    b = false;
    a = b and false;
    a = b or true;
    
    call printInt(x); // 2
    call printInt(y); // 3
    call printInt(z); // 7

    call printBool(a); // true
    call printBool(b); // false

}.